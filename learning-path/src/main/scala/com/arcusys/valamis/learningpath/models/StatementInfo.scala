package com.arcusys.valamis.learningpath.models

import org.joda.time.DateTime

case class StatementInfo(verbId: String,
                         objectId: String,
                         timeStamp: DateTime)
